apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: locust
spec:
  hosts:
    - "locust.financeiro.com"
  gateways:
    - gateway
  http:
    - route:
        - destination:
            host: locust-master-svc.stress.svc.cluster.local
            port:
              number: 8089
